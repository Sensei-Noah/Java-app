pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    //repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) //hack solution
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "javaprojectNojus"
include ':app'
